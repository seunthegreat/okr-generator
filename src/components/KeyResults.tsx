import React from 'react';
import { text } from '../style';

type Props = {}

const KeyResults = (props: Props) => {
  return (
    <div>
      <p className={`${text.body}`}>
        Result
      </p>
   </div>
  )
};

export default KeyResults;